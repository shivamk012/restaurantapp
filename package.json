{
  "name": "Food-Stop",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "heroku-postbuild": "cd ./client && npm install --only=dev && npm install && npm run build",
    "start": "node server/index.js"
  },
  "engines": {
    "npm": "8.1.0",
    "node": "16.13.0"
  },
  "dependencies": {
    "heroku": "^7.60.2"
  }
}
